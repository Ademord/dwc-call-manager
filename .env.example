# Reference only: set variables in the shell. npm scripts do not auto-load .env.
# Defaults work without any variables or credentials.
PORT=4318
DWC_DB_PATH=work/local-prototype.sqlite
# Optional Playwright channel; Windows defaults to installed Edge.
DWC_BROWSER_CHANNEL=msedge
